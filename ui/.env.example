# API Configuration
# The backend API URL. If not set, will use window.location.origin (same origin as frontend)
# For development with separate backend server, uncomment and set:
# VITE_API_URL=http://localhost:8181
