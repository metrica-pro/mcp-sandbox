[project]
name = "mcp-sandbox"
version = "0.2.0"
description = "Lightweight MCP code execution sandbox"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.12",
    "fastmcp>=2.2.0",
    "uvicorn>=0.34.0",
]

[project.scripts]
mcp-sandbox = "main:main"
